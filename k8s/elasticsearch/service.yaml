apiVersion: v1
kind: Service
metadata:
  name: elasticsearch-service
  namespace: __FO_NAMESPACE__
spec:
  selector:
    app: elasticsearch
  ports:
    - name: http
      port: 9200
      targetPort: 9200
    - name: transport
      port: 9300
      targetPort: 9300
  clusterIP: None  # Headless for StatefulSet DNS
---
apiVersion: v1
kind: Service
metadata:
  name: elasticsearch
  namespace: __FO_NAMESPACE__
spec:
  selector:
    app: elasticsearch
  ports:
    - port: 9200
      targetPort: 9200
