[project]
name = "anvil"
version = "1.0.0"
description = "Citadel modules (Anvil) — analyzers"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
    { name = "Citadel" },
]
# runtime deps tracked in requirements.txt
dependencies = []
