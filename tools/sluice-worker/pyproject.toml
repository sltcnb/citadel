[project]
name = "citadel-processor"
version = "1.0.0"
description = "Citadel processor — background worker"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
    { name = "Citadel" },
]
# runtime deps tracked in requirements.txt
dependencies = []
